{
  "name": "zigshape",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "dev": "bun --cwd apps/web run dev",
    "cli": "bun --cwd apps/cli run start"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.3"
  }
}
